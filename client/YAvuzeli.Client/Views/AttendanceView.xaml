<UserControl x:Class="YAvuzeli.Client.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Language="tr-TR" FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Background="White" CornerRadius="8" BorderBrush="#DDE5EF" BorderThickness="1" Padding="18" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Yoklama başarı oranı" Foreground="#64748B"/>
                <TextBlock Text="{Binding PresentRateText}" FontSize="30" FontWeight="SemiBold" Foreground="#225CB5" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="22"/><ColumnDefinition Width="340"/></Grid.ColumnDefinitions>
            <Border Background="White" CornerRadius="8" BorderBrush="#DDE5EF" BorderThickness="1" Padding="18">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                        <Button Content="Yenile" Command="{Binding RefreshCommand}" Padding="13,8" Margin="0,0,8,0"/>
                        <Button Content="Düzenle" Command="{Binding EditCommand}" Padding="13,8" Margin="0,0,8,0"/>
                        <Button Content="Sil" Command="{Binding DeleteCommand}" Padding="13,8" Foreground="#B42318"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Attendances}" SelectedItem="{Binding SelectedAttendance}" AutoGenerateColumns="False"
                              IsReadOnly="True" CanUserAddRows="False" RowHeight="36" AlternatingRowBackground="#F8FAFC">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Öğrenci" Binding="{Binding StudentName}" Width="*"/>
                            <DataGridTextColumn Header="Ders" Binding="{Binding CourseTitle}" Width="*"/>
                            <DataGridTextColumn Header="Tarih" Binding="{Binding AttendanceDate, StringFormat=d}" Width="120"/>
                            <DataGridCheckBoxColumn Header="Geldi" Binding="{Binding IsPresent}" Width="70"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="#DDE5EF" BorderThickness="1" Padding="20">
                <StackPanel IsEnabled="{Binding IsReady}">
                    <TextBlock Text="Yoklama kaydı" FontSize="19" FontWeight="SemiBold"/>
                    <TextBlock Text="Öğrenci *" Margin="0,18,0,4"/>
                    <ComboBox ItemsSource="{Binding Students}" SelectedValue="{Binding StudentId}" SelectedValuePath="Id" DisplayMemberPath="Name" Padding="6"/>
                    <TextBlock Text="Ders *" Margin="0,12,0,4"/>
                    <ComboBox ItemsSource="{Binding Courses}" SelectedValue="{Binding CourseId}" SelectedValuePath="Id" DisplayMemberPath="Name" Padding="6"/>
                    <TextBlock Text="Tarih" Margin="0,12,0,4"/>
                    <DatePicker SelectedDate="{Binding AttendanceDate}" Padding="4"/>
                    <CheckBox Content="Derse geldi" IsChecked="{Binding IsPresent}" Margin="0,16,0,18"/>
                    <Button Content="{Binding SaveLabel}" Command="{Binding SaveCommand}" Background="#225CB5" Foreground="White" Padding="13,9"/>
                    <Button Content="Yeni / Formu temizle" Command="{Binding NewCommand}" Margin="0,10,0,0" Padding="13,9"/>
                </StackPanel>
            </Border>
        </Grid>
        <Border Grid.Row="2" Background="#FFF7ED" BorderBrush="#FED7AA" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,14,0,0">
            <TextBlock Text="{Binding Message}" Foreground="#8A351B"/>
        </Border>
    </Grid>
</UserControl>
